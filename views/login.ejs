<% include include/header.ejs %>
	<div class="container login">
		<form action="/loginForm" method="post">
			<fieldset>
				<legend>로그인</legend>
		    	<label for="userId">아이디</label><input type="text" id="userId" name="username" autocomplete="off" placeholder="아이디" />
		    	<label for="userPw">비밀번호</label><input type="password" id="userPw" name="password" autocomplete="off" placeholder="비밀번호" />
		    	<input type="submit" class="btn" value="로그인" />
		    	<ul>
		        	<li><a href="#n">아이디</a>ㆍ<a href="#n">비밀번호 찾기</a></li>
		        	<li><a href="/join">회원가입</a></li>
				</ul>
			</fieldset>
		</form>
    </div>
    <script>
		$(function() {
			function validation() {
				if ($("#userId").val() === "") {
					$("#userId").focus();
					return;
				} if ($("#userPw").val() === "") {
					$("#userPw").focus();
					return;
				} 
				$("form").submit();
			}
			$("input").keydown(function(key) {
				if(key.keyCode == 13) validation();
			});
			$(".btn").click(function(e) {
				e.preventDefault();
				validation();
			});
		});
	</script>
<% include include/footer.ejs %>

