<% include ../../include/header.ejs %>

<section class="bg_sub_2 bg_securitycenter">
    <div>
        <h2>보안정보</h2>
        <strong>보안칼럼</strong>
    </div>
</section>

<section class="container">
    <ul class="breadcrumb">
        <li><a href="/">Home</a><i>&#62;</i></li>
        <li><a href="/securityCenter/news/1">보안센터</a><i>&#62;</i></li>
        <li><a href="/securityCenter/commonSense/1">보안정보</a><i>&#62;</i></li>
        <li>보안칼럼</li>
    </ul>
</section>

<section class="container board_view">
    <div class="view_head">
        <p class="lt"><%=result.contents.title%></p>
        <span class="gt"><%=moment(result.contents.reg_timestamp).format("YYYY-MM-DD")%></span>
    </div>
    <div class="view_body">
        <%-result.contents.content%>
    </div>
    <div class="view_option">
        <p>
            <span>이전글</span>
            <% if(result.next == null){%>
                <a href="#">이전글이 없습니다.</a>
            <% } else { %>
                <a href="/securityCenter/column/view/<%=result.next.id%>"><%=result.next.title%></a>
            <% } %>
        </p>
        <p>
            <span>다음글</span>
            <% if(result.prov == null){%>
                <a href="#">다음글이 없습니다.</a>
            <% } else { %>
                <a href="/securityCenter/column/view/<%=result.prov.id%>"><%=result.prov.title%></a>
            <% } %>

        </p>
    </div>
    <button onclick="window.location.href='/securityCenter/column/1'" id="listButton">목록</button>
</section>
<% include ../../include/footer.ejs %>
